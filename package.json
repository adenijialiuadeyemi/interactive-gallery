{
  "name": "interactive-gallery-monolith",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd backend && npm install && npm run build && npm start",
    "build": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
